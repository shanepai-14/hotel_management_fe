import { Typography } from '@mui/material';

const Billing = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Billing
      </Typography>
      {/* Add billing content */}
    </div>
  );
};

export default Billing;