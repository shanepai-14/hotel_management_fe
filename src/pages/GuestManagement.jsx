import { Typography } from '@mui/material';

const GuestManagement = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Guest Management
      </Typography>
      {/* Add guest management content */}
    </div>
  );
};

export default GuestManagement;